# Built/cloned artifacts live inside each integration folder — large and fully
# reproducible via each integration's install.sh. What we TRACK is the structure:
# integration.json, NOTICE.md, install.sh, README, and the Framer extension files.
*/src/
*/.venv/
*/node_modules/
*/dist/
*/build/
framer/_metadata/
*.log
