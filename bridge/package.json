{
  "name": "clone-frame-hub-bridge",
  "version": "0.3.9",
  "private": true,
  "type": "module",
  "description": "Local daemon that gives the CLONE FRAME HUB terminal a real shell + the user's own model (BYOK, any provider). Deps: ws, node-pty, imapflow, nodemailer, mailparser.",
  "license": "MIT",
  "bin": {
    "hub-bridge": "./hub-bridge.mjs"
  },
  "scripts": {
    "start": "node hub-bridge.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "imapflow": "^1.4.6",
    "mailparser": "^3.9.14",
    "node-pty": "^1.1.0",
    "nodemailer": "^9.0.3",
    "ws": "^8"
  }
}
